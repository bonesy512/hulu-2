import Head from 'next/head'
import Image from 'next/image'
import Link from 'next/link'
import Header from '../components/Header';

export default function Home() {
  return(
    <div>
        <Head>
          <title>Oceans 22</title>
        </Head>
        <h1>Lets build your Brand!</h1>
        <Header />
        {/* Sidebar */}

        {/* Results */}
    </div>
  );
}
